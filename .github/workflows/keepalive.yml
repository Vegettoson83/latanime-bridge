name: Keepalive Ping

on:
  schedule:
    - cron: '*/14 * * * *'  # every 14 minutes
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping bridge
        run: |
          curl -f https://latanime-bridge.onrender.com/health || echo "Bridge waking up..."
